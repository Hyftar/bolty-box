<p id="notice"><%= notice %></p>

<h1>Directories</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Users</th>
      <th>Assets</th>
      <th>Public</th>
      <th>Owner</th>
      <th>Parent</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @directories.each do |directory| %>
      <tr>
        <td><%= directory.name %></td>
        <td><%= directory.users %></td>
        <td><%= directory.assets %></td>
        <td><%= directory.public %></td>
        <td><%= directory.owner %></td>
        <td><%= directory.parent %></td>
        <td><%= link_to 'Show', directory %></td>
        <td><%= link_to 'Edit', edit_directory_path(directory) %></td>
        <td><%= link_to 'Destroy', directory, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Directory', new_directory_path %>
